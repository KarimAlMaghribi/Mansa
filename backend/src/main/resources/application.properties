# ? bisherige Einstellungen ?

# JPA / Hibernate ? künftig keine automatischen DDL-Änderungen mehr
spring.jpa.hibernate.ddl-auto=none

# Flyway aktivieren und Baseline erzwingen
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
# (Optional, falls Du nicht mit Version 1 baseline?n willst)
# spring.flyway.baseline-version=1
# Ordner mit Deinen Migrationsskripten
spring.flyway.locations=classpath:db/migration

# SQL-Logging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
